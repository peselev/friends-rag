# Core
chromadb==0.5.23
openai==2.38.0
anthropic==0.104.1
python-dotenv==1.0.1
requests==2.32.3

# UI
streamlit==1.40.0

# Retrieval
rank-bm25==0.2.2

# Reranker (also brings torch, transformers, tokenizers)
sentence-transformers==5.5.1

# Numerics (numpy comes via chromadb but pin to avoid surprises)
numpy==2.4.6